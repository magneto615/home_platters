# Netlify configuration file

# Build settings
[build]
  publish = "."

# Form settings
[build.processing]
  skip_processing = false

# Ensure forms are properly handled
[[plugins]]
  package = "@netlify/plugin-forms"
